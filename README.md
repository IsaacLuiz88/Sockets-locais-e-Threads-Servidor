# Servidor Local com Sockets e Threads

Este projeto implementa um **servidor local** em Python utilizando **sockets** e **threads**. O servidor é capaz de:

- Gerenciar múltiplas **conexões simultâneas**.
- Identificar cada **cliente** por meio de um **identificador único**.
- Exibir no **console** as mensagens enviadas pelos clientes.

## Como Executar

1. Certifique-se de ter o **Python 3.x** instalado.
2. Clone o repositório:
   ```bash
   git clone <URL_DO_REPOSITORIO>
